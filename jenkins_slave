pipeline {
    agent any

    stages {
        
        stage('Welcome to slave'){
            steps{
                sh ("python myscript.py ${params.NAME} --age ${params.Ages}")
            }
        }
    }
}
